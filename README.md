# Iris Demo
A simple Python script that demonstrates training a logistic regression model on the Iris dataset and making predictions.

## Usage
1. Install dependencies: `pip install scikit-learn numpy`
2. Run the script: `python iris_demo.py`

The script will output a prediction for the example measurement [5.1, 3.5, 1.4, 0.2].